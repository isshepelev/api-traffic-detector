version: "3.9"
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: "api-traffic-detector"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    ports:
      - "5432:5432"